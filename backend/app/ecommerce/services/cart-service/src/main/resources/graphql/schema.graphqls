type Query {
    cart: [Cart]
}

type Cart {
    id: ID!
    name: String
    createdAt: String
    customer: String
    status: String
    cartItems: [CartItem]
}

type CartItem {
    id: ID!
    quantity: Int
    product: String
    cartId: String
    createdDate: String
}
