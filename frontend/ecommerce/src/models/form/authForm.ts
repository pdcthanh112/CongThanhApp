export type LoginFormType = {
    email: string
    password: string
    fcmToken?: string;
} 

export type RegisterFormType = {
    email: string
    name: string
    phone: string
    password: string
}