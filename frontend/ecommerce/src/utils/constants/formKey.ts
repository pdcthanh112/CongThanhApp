export enum eLoginFormKey {
  Email = 'email',
  Password = 'password',
}

export enum eRegisterFormKey {
  Email = 'email',
  Name = 'name',
  Password = 'password',
  Phone = 'phone',
}

export enum eForgetPasswordFormKey {
  Email = 'email',
}

export enum eResetPasswordFormKey {
  Password = 'password',
  Confirm = 'confirm',
}

export enum eChangePasswordFormKey {
  CurrentPassword = 'currentPassword',
  NewPassword = 'newPassword',
  ConfirmPassword = 'confirmPassword',
}
